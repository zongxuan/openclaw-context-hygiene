# context-hygiene 插件详解

> 写给小鲨鱼 🦈 的小白教程

------

## 一、这个插件是干什么的？

**一句话回答：让 AI 对话长期保持又快又稳。**

你可以把 AI 对话想象成一个"记忆"：

- 对话越短，AI 反应越快（省 token = 省钱）
- 对话越长，AI 越容易"犯糊涂"（上下文太大容易出错）

这个插件就是一个**自动清理员**，每次你发消息给 AI 之前，它都会先快速清理一遍，把没用的东西扔掉，保持对话清爽。

------

## 二、为什么需要这个插件？

### 没有它会发生什么？

想象一个场景：你让 AI 帮你读文件

```
你: 读取 /tmp 目录
AI: [返回文件列表]  ← 这里有 50 个文件的详细内容，占用很多"记忆空间"

你: 读取 /tmp 目录   ← 同样的命令
AI: [再次返回文件列表]  ← 但这次 AI 还在记着上次的结果，"记忆空间"被浪费了
```

随着对话变长，重复的内容越来越多，AI 的"脑子"被垃圾填满：

- 速度变慢
- 容易出错
- 钱包变瘪（token 费 = 越来越贵）

### 有这个插件之后

插件在每次 AI 回复之前，自动清理掉重复的内容：

```
清理前: 15 条消息 → 清理后: 10 条消息
减少了 33% 的"垃圾"
```

AI 每次只处理真正有用的信息，速度更快、更稳定。

------

## 三、三层压缩是什么？

插件有三层防护网，从轻到重：

### 第一层：微清理（Layer 0）— 每次都触发，最快

**比喻：** 你在整理房间

- 把地上的 `[zombie message]` 纸片扔掉
- 把重复的文件（连续两次读同一个文件）合并成一份
- 把空的占位符扔掉

**速度：** ~5毫秒（眨眼之间）
**消耗：** 几乎为零

### 第二层：模型总结（Layer 1）— 需要时触发，较慢

**比喻：** 房间实在太乱了，需要叫保洁阿姨来打扫

阿姨（AI 模型）会把很久以前的事情整理成一段摘要，比如：

> "之前用户读了很多次 /tmp 目录，主要是看临时文件"

**速度：** 10-30秒
**消耗：** 调用一次 AI 模型（有成本，但比上下文爆满划算）

### 第三层：激进压缩（Layer 2）— 极端情况，最激进

**比喻：** 房间已经乱到阿姨也整理不过来，必须"断舍离"

把所有中间过程的记录扔掉，只保留最终结果。

**速度：** ~5毫秒（但会丢失一些细节）
**触发条件：** 前两层都处理不了的时候

------

## 四、安装这个插件有什么用？

### 实际效果

| 场景             | 没有插件           | 有插件             |
| ---------------- | ------------------ | ------------------ |
| 重复读取同一文件 | 每次都记，越来越慢 | 只记一次，明显更快 |
| 对话变长         | 越来越慢，容易出错 | 保持稳定速度       |
| Token 消耗       | 重复内容占空间     | 减少 20-40%        |
| 极端长对话       | 可能卡死           | 基本不会爆满       |

### 你能感受到的变化

1. **对话更流畅** — AI 反应速度稳定
2. **更省钱** — 减少不必要的 token 消耗
3. **长对话质量更稳定** — 不会因为上下文太大而"变笨"

------

## 五、安装麻烦吗？

**一点都不麻烦，一行命令：**

```bash
./install.sh
```

插件是完全"自包含"的，不需要装任何额外的东西。

------

## 六、技术原理（可选阅读）

如果你对技术感兴趣，下面是简化版原理：

### Hook 是什么？

OpenClaw 有一个"钩子"系统，可以在 AI 处理消息的前后插入自定义逻辑。

`context-hygiene` 插件注册了两个钩子：

```
用户发消息
    ↓
钩子1: before_prompt_build
  → snipCompact 清理垃圾
    ↓
AI 处理消息
    ↓
钩子2: session:compact:before
  → 如果需要压缩，运行 contextCollapse
    ↓
返回 AI 回复
```

### snipCompact 清理什么？

1. **`[zombie message]`** — 已经没用的占位符
2. **空的 throttle/warning 消息** — 空的提示信息
3. **连续重复的 tool_result** — 同一个操作结果被重复记录多次

### contextCollapse 清理什么？

1. **中间态工具结果** — 比如同一个文件被读取了 5 次，只保留最后一次
2. **跨对话的重复读取** — 同一文件在对话开头和结尾都被读取，只保留结尾的
3. **超大的输出** — 超过 2KB 的输出截断成摘要

------

## 七、常见问题

**Q: 插件会影响 AI 的回答质量吗？**
A: 不会。插件只清理"垃圾"，不会删除有用的内容。有用的信息会被保留。

**Q: 安装后需要维护吗？**
A: 不需要。插件自动运行，完全不需要人工干预。

**Q: 如果我想卸载呢？**
A: 

```bash
rm -rf ~/.openclaw/hooks/context-hygiene
openclaw gateway restart
```

**Q: 这个插件是哪来的？**
A: 基于 Claude Code（一个著名的 AI 编程工具）的架构研究开发，OpenClaw 可以完美复刻。

------

*小鲨鱼出品 🦈 · 2026-04-04*